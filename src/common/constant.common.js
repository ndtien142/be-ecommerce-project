const IMPORT_RECEIPT_STATUS = Object.freeze({
    requested: 'requested',
    approved: 'approved',
    processing: 'processing',
    completed: 'completed',
    rejected: 'rejected',
    received: 'received',
    returned: 'returned',
});

module.exports = {
    IMPORT_RECEIPT_STATUS,
};
